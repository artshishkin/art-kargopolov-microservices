spring:
  application:
    name: users-ws
  cloud:
    config:
# We can skip name because it is match the application name
      name: users-ws
      uri: http://localhost:8012
      fail-fast: true

